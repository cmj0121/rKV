apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rkv.fullname" . }}
  labels:
    {{- include "rkv.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "rkv.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "rkv.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "rkv.serviceAccountName" . }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      terminationGracePeriodSeconds: 30
      containers:
        - name: rkv
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - serve
            - --bind
            - {{ .Values.server.bind | quote }}
            - --port
            - {{ .Values.server.port | quote }}
            {{- if .Values.server.allowAll }}
            - --allow-all
            {{- end }}
            - --body-limit
            - {{ .Values.server.bodyLimit | quote }}
            - --timeout
            - {{ .Values.server.timeout | quote }}
            - --db
            - /data
            {{- if .Values.server.ui }}
            - --ui
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.server.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: data
              mountPath: /data
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "rkv.fullname" . }}
          {{- else }}
          emptyDir: {}
          {{- end }}
