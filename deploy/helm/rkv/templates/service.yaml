apiVersion: v1
kind: Service
metadata:
  name: { { include "rkv.fullname" . } }
  labels: { { - include "rkv.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "rkv.selectorLabels" . | nindent 4 } }
