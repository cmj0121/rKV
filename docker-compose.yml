services:
  rkv:
    build: .
    platform: linux/amd64
    ports:
      - "8321:8321"
    volumes:
      - .data:/data
    command:
      ["serve", "--bind", "0.0.0.0", "--db", "/data", "--allow-all", "--ui"]
